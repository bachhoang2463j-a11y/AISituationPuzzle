# TurtleSoup 多轮开发计划

## 0. 文档目的

本计划用于把海龟汤前端拆成多个可独立验收的开发轮次，避免一次同时实现视觉、状态机、酒馆适配、AI 提示词、多人角色、便签和持久化。

每轮只解决一个主要风险。上一轮未通过验收时，不进入下一轮，不叠加新功能。

本计划遵守以下项目约束：

- `UI_design.html` 是视觉基线，不允许被功能实现替换成另一套布局。
- 最终交付仍是单个 HTML 文件，但代码必须按命名空间或模块区域组织。
- 游戏过程默认只存在内存；API 配置、头像和角色人设才允许持久化。
- 主持人可以看到汤底；伙伴、玩家和闲聊主持人不能看到汤底或内部思维。
- AI 只能返回结构化结果，不能直接修改前端状态。
- 玩家是每轮唯一触发者，禁止 AI 自动递归开启下一轮。
- 重大架构变更先确认，编码轮次结束后追加 `LOG.md` 和 `LOG-INDEX.md`。

## 1. 当前基线

### 1.1 保留文件

- `UI_design.html`：视觉参考和本地模拟交互参考，第一阶段只读，不直接修改。
- `SPEC.md`：目标架构和最终验收标准。
- `Coding rule.md`：施工和日志规则。

### 1.2 当前问题

- `TurtleSoup.html` 使用了与视觉 demo 不同的 DOM 和 CSS 布局。
- 缺少酒馆接口时只有固定汤面回退，没有完整的本地问答闭环。
- 无 `<Puzzle>` 时没有默认的三名伙伴展示，页面退化为空角色列表。
- 伙伴请求、主持人请求、便签总结和闲聊逻辑同时存在，尚未经过分阶段验证。
- `peerReal`、`peerSimple`、`hostAnswer` 等方法存在重复定义，后定义会覆盖前定义。
- 当前没有针对页面启动、Mock 回合、提示词隔离和酒馆能力缺失的自动化验收脚本。

### 1.3 目标基线

新的 `TurtleSoup.html` 必须先恢复 `UI_design.html` 的主要视觉结构，再逐步替换其中的模拟数据层。功能开发不得先创建另一套简化 UI。

## 2. 每轮固定流程

每轮施工都必须执行以下步骤：

1. 明确本轮只解决一个主要问题。
2. 写出本轮不做的功能，防止范围膨胀。
3. 修改尽量限制在 `TurtleSoup.html` 和本轮需要的测试文件。
4. 先运行静态检查，再运行本轮验收场景。
5. 验收通过后追加 `LOG.md` 和 `LOG-INDEX.md`。
6. 验收失败时只修复本轮问题，不顺便加入下一轮能力。
7. 如果发现需要改变状态模型、提示词协议或持久化边界，暂停编码并先确认架构变更。

每轮完成的最低证据包括：

- 页面是否能启动；
- 本轮入口是否可操作；
- 成功路径是否正确；
- 失败路径是否仍保持可操作；
- 刷新或重置后是否符合本轮预期；
- 控制台是否没有新增未处理错误。

## 3. 分轮计划

### 第 0 轮：建立基线和测试夹具

**目标**

确认视觉 demo、当前实现和目标规范的差异，建立不会依赖酒馆环境的测试输入。

**只做这些事**

- 记录 `UI_design.html` 的关键 DOM ID、按钮和弹窗入口。
- 创建一个固定的本地 `<Puzzle>` fixture。
- 定义最小的 Mock 响应数据：汤面、主持人回答、一个伙伴发言。
- 定义页面启动检查和控制台错误检查方法。

**本轮不做**

- 不接 API。
- 不读世界书。
- 不实现新的游戏逻辑。
- 不修改视觉 demo。

**验收标准**

- fixture 能被独立读取。
- Mock 数据能被解析成明确的对象。
- 开发者可以在没有 SillyTavern 的浏览器中重复启动测试。
- 验收步骤已经写入本文件或测试说明，不依赖临时记忆。

**完成产物**

- 固定测试数据。
- 启动检查清单。
- 一条日志记录。

### 第 1 轮：恢复视觉基线

**目标**

以 `UI_design.html` 为基础恢复圆桌、座位、气泡、便签墙、记录栏、移动端 Tab 和现有弹窗。

**只做这些事**

- 从视觉 demo 恢复主要 HTML 结构和 CSS。
- 保留原有按钮、输入框、弹窗和响应式布局。
- 将静态文本保留为 demo 数据，不接真实业务状态。
- 为后续 View 层补充稳定的 `data-*` 或 ID 钩子。

**本轮不做**

- 不接 Tavern API。
- 不实现 `<Puzzle>` 读取。
- 不实现 AI 生成。
- 不实现便签总结和持久化。

**验收标准**

- 页面视觉结构与 `UI_design.html` 一致：圆桌舞台、五个座位、气泡、便签墙、记录区均存在。
- API、设置、人设编辑、全屏和移动端 Tab 入口存在且可打开。
- 页面启动没有白屏、布局没有水平溢出。
- 桌面和窄屏各检查一次。
- 本轮不引入新的状态机。

**停止条件**

- 任何核心视觉区域缺失时，不进入第 2 轮。

### 第 2 轮：本地 Mock 问答闭环

**目标**

先让视觉页面在独立浏览器中完成一轮可玩的“玩家提问 -> 主持人回答”。

**只做这些事**

- 建立最小 `GameState`：汤面、公开记录、角色气泡、当前阶段。
- 将视觉 demo 的 `playerSpeak()` 和 `triggerSpeakerBubble()` 接到状态更新。
- 保留 `hostEvaluate()` 作为 `MockAdapter`，只用于本轮测试。
- 支持发送、Enter 发送、重置。

**本轮不做**

- 不接真实模型。
- 不加入 AI 伙伴。
- 不加入随机抽样。
- 不加入便签自动总结。

**验收标准**

- 输入问题后，玩家气泡和记录区立即更新。
- Mock 主持人在延迟后返回一次 `是 / 否 / 无关`。
- 主持人气泡、发言状态和记录区同步更新。
- 空输入不会新增记录。
- 重复点击不会并发产生两次同一回答。
- 重置后恢复初始 demo 状态。

**完成定义**

独立浏览器没有酒馆接口时，用户仍能完整操作至少一轮，不能只显示固定汤面。

### 第 3 轮：状态模型和 View/Action 分离

**目标**

把 UI 渲染和游戏状态分开，避免后续接 API 时重写视觉层。

**只做这些事**

- 明确 `GameState`、`GameAction` 和阶段枚举。
- 建立单向流程：Action -> Store -> View render。
- 将角色、记录、便签和回合字段集中到状态对象。
- 为请求增加 `requestId` 和 `abort` 状态。

**本轮不做**

- 不增加任何新用户功能。
- 不改变 Mock 问答的外部行为。
- 不加入持久化。

**验收标准**

- 同一操作不会重复绑定事件。
- 调用 `render()` 不会丢失输入内容或重复追加记录。
- 状态可以序列化用于调试，但不会写入酒馆变量。
- 当前阶段非法时，发送按钮不会触发新回合。
- 第 2 轮全部验收场景仍然通过。

### 第 4 轮：TavernAdapter 能力探测

**目标**

将酒馆相关全局函数集中到适配层，并在接口缺失时继续使用 MockAdapter。

**只做这些事**

- 定义 `TavernAdapter`：消息读取、生成、变量读写、停止生成、事件订阅。
- 启动时输出能力诊断状态。
- 统一同步返回值、Promise 返回值和错误格式。
- 定义 `MockAdapter` 与 `TavernAdapter` 的共同接口。

**本轮不做**

- 不读取世界书角色。
- 不生成真实汤面。
- 不实现多角色请求。

**验收标准**

- 完整酒馆接口存在时，能力诊断显示可用。
- 任一接口缺失时，页面不白屏并继续使用 Mock 闭环。
- `stop`、变量读写和消息读取失败时，错误会进入诊断区域或 Toast。
- UI 和游戏逻辑不再直接调用 `window` 下的酒馆函数。

### 第 5 轮：Puzzle 输入和固定主持人题目

**目标**

只实现 `<Puzzle>` 读取和主持人题目加载，不引入世界书和伙伴。

**只做这些事**

- 读取 `游戏数据栏.主持人` 和连续的 `玩家N`。
- 校验缺少主持人、玩家编号断裂和同名角色。
- 从固定题目响应或 Tavern 生成结果生成汤面和汤底。
- 在状态中严格区分 `publicSoup` 与 `secret`。

**本轮不做**

- 不读取世界书。
- 不加载角色人设。
- 不实现伙伴发言。
- 不显示汤底。

**验收标准**

- 合法 `<Puzzle>` 能建立本局角色名单。
- 非法 `<Puzzle>` 显示可操作错误，不白屏。
- 主持人请求可以访问汤底。
- 页面 DOM、记录和伙伴提示词中不存在完整汤底。
- 刷新后游戏状态重新开始。

### 第 6 轮：真实主持人问答

**目标**

用真实结构化主持人请求替换 Mock 主持人，但仍然没有 AI 伙伴。

**只做这些事**

- 定义 `host-answer-v1` 响应协议。
- 建立玩家问题的 `question_id`。
- 校验回答字段、判定类型和问题 ID。
- 成功、格式错误、超时和停止都回到可操作状态。

**本轮不做**

- 不加入伙伴问题。
- 不加入简单/真实模式。
- 不加入便签总结。

**验收标准**

- 一次玩家问题最多产生一次主持人请求。
- 缺字段、重复 ID、未知 ID 或顺序错误的响应整批拒绝。
- 主持人失败后玩家可以重试。
- 汤底只进入主持人请求，不进入公共记录。
- 无 Tavern 时仍回退到第 2 轮 MockAdapter。

### 第 7 轮：单个 AI 伙伴

**目标**

只加入一名 AI 伙伴，验证最小多角色状态机。

**流程**

```text
awaiting-player
-> peer-thinking
-> host-thinking
-> awaiting-player
```

**本轮只做**

- 一个伙伴独立请求一次公开发言。
- 伙伴只能看到汤面、公开记录和便签公共区块。
- 主持人一次回答玩家问题和伙伴问题。
- 伙伴问题显示为建议，不自动再次提交。

**本轮不做**

- 不做三伙伴。
- 不做 50% 抽样。
- 不做简单模式。
- 不做闲聊。

**验收标准**

- 一次玩家动作最多触发一次伙伴请求和一次主持人请求。
- 伙伴请求中没有 `secret.truth`、完整汤底或主持人私密响应。
- 伙伴失败不会阻塞玩家继续提问。
- 主持人回答回到 `awaiting-player`。
- 停止请求后不会自动开启下一轮。

### 第 8 轮：三个伙伴和真实模式

**目标**

在单伙伴稳定后扩展为默认三个伙伴，并实现逐角色请求。

**只做这些事**

- 从 `<Puzzle>` 建立 0~N 个伙伴。
- 每轮独立以 50% 概率抽样。
- 按固定队列顺序显示伙伴发言。
- 每个伙伴每轮最多发言一次。
- 记录抽样结果，重试时不重新抽样。

**本轮不做**

- 不做合并请求。
- 不做闲聊。
- 不做自动总结。

**验收标准**

- 0 个、1 个和 3 个伙伴都能运行。
- 每个伙伴最多产生一条本轮公开发言。
- 真实模式请求数量与抽样队列一致。
- 部分伙伴失败不会丢失其他成功结果。
- 伙伴批次完成后只能进入主持人阶段或等待玩家阶段。

### 第 9 轮：简单模式合并请求

**目标**

增加一次请求扮演多个伙伴的简单模式，并严格校验角色 ID。

**只做这些事**

- 用一个结构化响应承载抽样队列中的多个伙伴。
- 校验每个抽样伙伴恰好出现一次。
- 发生角色缺失、重复或代答时整批拒绝。
- 在设置中切换真实模式和简单模式。

**本轮不做**

- 不实现主持人和伙伴合并请求。
- 不实现内部思维传递。
- 不改变公共 UI 布局。

**验收标准**

- 简单模式只有一次伙伴生成请求。
- 返回 ID 集合必须与抽样队列完全相等。
- 无效响应不会错位写入角色气泡。
- 真实模式回归测试仍然通过。

### 第 10 轮：中央便签墙和总结者

**目标**

让主持人回答之后更新公开便签，但不允许 AI 直接覆盖便签墙。

**只做这些事**

- 支持四类便签：确认事实、重要线索、核心疑问、已排除。
- 总结者只返回 `add/update/remove/move` 操作。
- 校验 `round_id`、`summarizer_id`、`operation_id` 和便签 ID。
- 支持玩家编辑、移动和删除，并保留 `source: player`。
- 下一轮公共提示词包含 `TURTLE_PUBLIC_NOTE_WALL`。

**本轮不做**

- 不做游戏过程持久化。
- 不做结束总结回写酒馆。

**验收标准**

- 非法便签操作整批拒绝。
- 玩家便签不会被标记为总结者确认事实。
- 便签变化能反映到 UI 和下一轮公共上下文。
- 便签操作不会暴露汤底。

### 第 11 轮：闲聊模式

**目标**

实现输入级闲聊，确保闲聊不改变解题回合。

**只做这些事**

- 当前输入勾选闲聊时，主持人和所有启用伙伴各回应一次。
- 闲聊请求不注入汤底。
- 闲聊只新增聊天记录，不新增问题账本、不更新便签、不消耗游戏回合。

**验收标准**

- 闲聊不会触发主持人判定。
- 闲聊响应中不存在汤底。
- 闲聊结束后仍能继续上一轮解题。
- 真实模式和简单模式的闲聊响应数量符合配置。

### 第 12 轮：配置持久化和事件生命周期

**目标**

完成配置保存、聊天切换和事件清理，同时保持游戏过程刷新清空。

**只做这些事**

- 持久化 API 配置、头像和角色人设。
- 监听聊天切换或消息变化，清空并重建当前局。
- 清理事件订阅和请求句柄。
- 明确 API Key 不进入持久化变量。

**验收标准**

- 刷新后只恢复允许持久化的配置。
- 汤面、汤底、记录和便签不会跨刷新恢复。
- 切换聊天不会带入上一局状态。
- 重复初始化不会重复订阅事件。
- 损坏变量会降级为默认配置，不白屏。

### 第 13 轮：兼容性和发布验收

**目标**

在交付前验证不同运行环境，不新增业务功能。

**测试矩阵**

- 完整酒馆助手接口。
- 缺少 `generateRaw`，仅有 `generate`。
- 缺少世界书。
- 缺少事件 API。
- 没有生成接口的独立浏览器。
- 没有 `<Puzzle>`。
- 0 个、1 个和 3 个伙伴。
- 桌面宽屏和窄屏。
- 正常回答、格式错误、超时、停止和重试。

**验收标准**

- 所有环境都能显示可操作 UI。
- 不因单个能力缺失而白屏。
- 伙伴提示词永远不含汤底。
- 主持人聚合回答严格覆盖问题账本。
- 所有日志、文档和最终 HTML 文件路径清晰。

## 4. 每轮统一验收清单

在任何轮次标记完成前，必须回答以下问题：

- 本轮新增了哪个用户可见能力？
- 本轮明确没有实现哪些能力？
- 没有酒馆接口时页面是否仍可操作？
- 错误、停止和重试是否回到合法状态？
- 是否出现重复请求、重复记录或重复事件订阅？
- 是否把汤底或内部响应泄露给公共 UI、伙伴或日志？
- 是否破坏了 `UI_design.html` 的视觉区域？
- 是否追加了对应的 `LOG.md` 和 `LOG-INDEX.md`？

如果其中任一答案不明确，本轮视为未完成。

## 5. 开发纪律和回滚规则

### 5.1 单轮范围限制

- 一轮最多引入一个新的外部依赖或接口族。
- 一轮最多改变一个主要状态流程。
- 一轮不同时修改视觉基线和 AI 协议。
- 一轮不同时加入新角色数量、新模式和新持久化字段。
- 不为了“顺手整理”重构相邻模块。

### 5.2 回滚点

每轮开始前记录：

- 当前 `TurtleSoup.html` 的可启动状态。
- 当前视觉截图或 DOM 检查结果。
- 当前日志位置。
- 本轮新增的接口和状态字段。

如果本轮无法恢复上一轮验收结果，只回滚本轮变更，不修改已经通过的轮次。

### 5.3 进入下一轮的门槛

只有满足以下条件，才允许进入下一轮：

- 本轮所有验收标准通过。
- 没有已知的白屏、死循环、重复请求或秘密泄露问题。
- 回归测试通过。
- 日志已追加。
- 本轮新增状态字段和接口已有明确用途。

## 6. 推荐的首个实际施工范围

下一次编码只执行第 1 轮和第 2 轮，不要同时接 Tavern API。

目标只有两个：

1. 恢复 `UI_design.html` 的原视觉结构。
2. 在独立浏览器中完成一次 Mock 的玩家提问和主持人回答。

当这两个目标通过后，再单独开始第 3 轮状态模型。这样每次改动都能立即看到结果，也不会再次出现“功能代码增加了，但页面和基本交互一起失效”的情况。
